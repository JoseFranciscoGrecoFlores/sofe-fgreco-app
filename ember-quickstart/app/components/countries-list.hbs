<div class="mx-md-5 mt-5 p-3 shadow rounded">
  <h3 class="text-center">Countries</h3>
  <div class="form-group row align-items-center">
    <label class="col-auto col-form-label" for="Order by">Order by:</label>
    <div class="col-auto">
      <select id="Order by" name="Order by" class="custom-select" {{on 'change' (fn this.onChangedOrderCriteria)}}>
        <option value="country">Country name</option>
        <option value="cases">Cases</option>
        <option value="todayCases">Today cases</option>
        <option value="deaths">Deaths</option>
        <option value="todayDeaths">Today deaths</option>
        <option value="recovered">Recovered</option>
        <option value="active">Active</option>
        <option value="critical">Critical</option>
        <option value="casesPerOneMillion">Cases per one million</option>
        <option value="deathsPerOneMillion">Deaths per one million</option>
      </select>
    </div>
  </div>
  <div class="table-responsive">
    <table class="table table-striped table-hover">
      <thead class="thead-dark">
        <tr>
          <th>Country name</th>
          <th>Cases</th>
          <th>Today cases</th>
          <th>Deaths</th>
          <th>Today deaths</th>
          <th>Recovered</th>
          <th>Active</th>
          <th>Critical</th>
          <th>Cases per one million</th>
          <th>Deaths per one million</th>
        </tr>
      </thead>
      <tbody>
        {{#each this.listToDisplay as |country|}}
        <tr>
          <td>{{country.country}}</td>
          <td>{{country.cases}}</td>
          <td>{{country.todayCases}}</td>
          <td>{{country.deaths}}</td>
          <td>{{country.todayDeaths}}</td>
          <td>{{country.recovered}}</td>
          <td>{{country.active}}</td>
          <td>{{country.critical}}</td>
          <td>{{country.casesPerOneMillion}}</td>
          <td>{{country.deathsPerOneMillion}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  <nav aria-label="Country navigation">
    <ul class="pagination justify-content-center">
      {{#each this.pagesToDisplay as |page|}}
      <li class="{{if page.active "active"}} page-item">
        <button class="page-link" {{action "onPageSelected" page.value}}>{{page.value}}</button>
      </li>
      {{/each}}
    </ul>
  </nav>
</div>